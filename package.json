{
  "name": "zoo-president",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "npm run build && npx cap sync android",
    "emu": "firebase emulators:start --only auth,functions,database,firestore --project zoo-app-d6cb4",
    "test:game": "node test/game.test.js"
  },
  "devDependencies": {
    "firebase-tools": "^15.27.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "firebase": "^11.0.0"
  }
}
