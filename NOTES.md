# 이번 파일에 들어간 것

## 새 그림
- 상단바 (프로필 · 티어 · 이름 · 점수 · 티켓 · 설정)
- 티켓 / 프로필 / 설정 / 끊김 아이콘, 칸 나누는 세로선

## 잡은 버그
- **판이 얼어붙던 버그.** 손에 카멜레온만 남은 사람이 선일 때 못 낸다고 판단해 계속 패스했다.
  카멜레온은 혼자 내면 13번으로 낼 수 있다. 5000판 시험으로 확인했다.
- **화면 애니메이션이 전부 죽어 있던 문제.** CSS 를 화면별로 묶을 때 @keyframes 앞에 화면
  이름이 붙어 무효가 됐다. 차례 깜빡임·카운트다운·등수 뒤집기 모두 안 돌고 있었다.

## 구조 변경
- 모든 수가 applyMove 한 곳을 거친다. 내 수·봇 수·(나중에) 남의 수가 같은 길로 들어와
  화면이 어긋날 여지를 없앴다.
- window.__net 이 있으면 서버로 보내고, window.__applyMove 로 밖에서 들어온 수를 받는다.
  지금은 __net 이 없어 봇전으로 돈다.

## 서버 (아직 화면과 연결 안 됨)
- functions/deck.js    덱과 규칙
- functions/index.js   서버 함수 4개 (배분 / 세금 / 봇 / 검증)
- src/lib/online.js    방 만들기 · 참가 · 실시간 동기화
- database.rules.json  실시간 데이터베이스 규칙

## 하셔야 할 것
1. Firebase 콘솔에서 **Realtime Database** 만들기 (위치: 싱가포르)
2. 나온 주소를 .env 의 VITE_FB_DB_URL 에 넣기
3. PC 화면: 양옆 배경과 전체 높이 적용됨. 확인만 하면 됨
