node_modules
dist
.env
# 안드로이드 프로젝트(android/)는 **깃에 올린다.**
# AdMob 앱 번호·아이콘·서명 설정이 거기 들어가서, 안 올리면 컴퓨터가 바뀔 때 다 잃는다.
# 대신 빌드 찌꺼기만 뺀다
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/.idea/
android/app/release/
android/app/*.keystore
android/app/*.jks
# 서명 비밀번호가 든 파일. 올라가면 남이 내 앱인 척 만들 수 있다
android/keystore.properties
functions/node_modules
.DS_Store

# 검사가 돌 때마다 esbuild 가 새로 만드는 임시 묶음. 소스가 아니다
test/_bundle*.mjs
firestore-debug.log
