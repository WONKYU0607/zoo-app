{
  "rules": {
    "rooms": {
      "$code": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },
    "hands": {
      "$code": { "$uid": { ".read": "auth != null && auth.uid === $uid", ".write": false } }
    }
  }
}
