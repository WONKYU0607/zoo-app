# 고친 것

## 방 만들기가 안 되던 원인
앱이 데이터베이스 주소를 안 넘겨서 기본(미국) 주소로 붙고 있었다.
콘솔에 "Database lives in a different region" 경고가 그것이다.
firebase.js 와 online.js 두 곳에서 싱가포르 주소를 쓰도록 고쳤다.

## 실패해도 조용하지 않게
방을 못 만들거나 못 들어가면 이유를 알림으로 띄운다.
로그인 전이면 "로그인이 필요합니다", 설정이 없으면 그렇게 나온다.

## 규칙도 손봤다
moves 하위 조건이 방 만들기까지 막을 수 있어 뺐다.
아래 내용을 콘솔 > Realtime Database > 규칙 에 붙여넣고 게시하거나,
firebase deploy --only database 로 올리면 된다.

# 확인 방법

npm install
npm run dev

브라우저 두 개(하나는 시크릿)로 열어 서로 다른 구글 계정으로 로그인.
한쪽에서 방을 만들고 나온 번호를 다른 쪽에 넣는다.
두 화면에 서로가 보이면 성공.

안 되면 F12 → Console 의 빨간 줄을 알려 주면 된다.
