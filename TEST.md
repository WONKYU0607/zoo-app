# 자동 시험 쓰는 법

한 게임을 처음부터 끝까지 자동으로 플레이해서 어디가 깨지는지 찾는다.
**에뮬레이터(내 컴퓨터 안의 가짜 서버)** 를 상대로 돌기 때문에
실제 Firebase 데이터는 전혀 건드리지 않는다.

## 준비 (한 번만)

    npm install
    cd functions && npm install && cd ..

## 돌리기

터미널 두 개가 필요하다.

**터미널 1 — 가짜 서버를 켠다**

    npm run emu

"All emulators ready" 가 뜰 때까지 기다린다.
처음 한 번은 실행 파일을 받느라 몇 분 걸린다.

**터미널 2 — 시험을 돌린다**

    npm run test:game

## 결과 보는 법

    [OK]   방 만들기  번호 4729
    [OK]   봇 채우기  자리 6명
    [OK]   판 시작  상태 playing
    [실패] 봇 수  ...

`[실패]` 가 나온 줄과 그 뒤의 설명을 그대로 알려 주면 된다.
마지막에 "통과 N / 실패 M" 이 찍힌다.

## 무엇을 확인하나

- 로그인, 방 만들기, 봇 채우기
- 서버가 카드를 80장 정확히 나누는지
- **남의 손패가 안 보이는지**
- 봇이 실제로 카드를 내는지
- 판이 멈추지 않고 끝나는지
- 세금 정산과 다음 판으로 넘어가는지
- 정해진 판 수를 채우고 게임이 끝나는지
- 점수가 계정에 반영되는지, 두 번 받지 않는지

## 화면까지 같이 보려면

에뮬레이터를 켠 채로

    npm run dev

로 앱을 열면 가짜 서버에 붙는다. 실제 데이터가 안 더러워지므로
마음껏 시험해도 된다. (붙이려면 src/lib/firebase.js 에 에뮬레이터 연결을
넣어야 한다 — 필요하면 요청할 것)
